# Jimu implementation baseline — accepted 2026-09-06

Architecture v1.0 is accepted under the owner's separately supplied Preference Policy v1. This is an acceptance receipt, not a replacement architecture. Routine architecture decisions remain frozen; reopen only the specific assumption contradicted by implementation evidence.

Accepted original archive SHA-256:
`bee57a87aee6ccf7494204276cd4cb0e072e88d7b5948c2805680529c5206c4d`

Implementation base: `f098aad41cbb995ed3e5a9c5ccab7c2160c42f0e`.
Procedure pin: `mastermindx-market-intelligence/Mastermind@ffbb2eb138cb3c3cb0d211973e0cf30a314b7520`, Skillpack v1.0.1 / bootstrap-major 1.

## Frozen implementation boundaries

Extend this macOS application and its existing `curator.sqlite` / GRDB migration owner. Keep the Windows inference worker separate and Android a thin observation/execution adapter. Do not introduce another database, queue, policy authority, generic autonomous screen agent or product-wide refactor. Observation, extracted facts, model judgments, recommendations, commands and verified effects remain different records. HelloTalk's no-social-action policy is unchanged.

The current delivery admits offline replay only. All engagement, message submission, scarce actions and purchasing remain disabled. Unknown or contradictory age never becomes visually estimated age. Personal preferences are configurable private input, not compiled defaults. Architecture acceptance and source code do not promote live autonomy.

## Privacy correction

GitHub reports this repository as PUBLIC, despite its description. Only generic implementation, neutral synthetic fixtures and redacted engineering records belong here. Do not commit owner-specific dating criteria, calibration labels, real profile media, private evidence, account/device identifiers or endpoints. The original architecture archive and private Preference Policy are not republished here. Repository visibility was not changed.

## Wave disposition

W0 and W1 are authorized by the owner's current instruction. W0's replay starter is the first source slice; native contract, platform permission, installed account entitlements and real-device proof remain unverified. W1 must deliver a working immutable offline inspector in the existing app, not just a schema. W0 live evidence gaps do not block synthetic/offline W1 work.

W2 may proceed in parallel once the common observation contract in `OBSERVATION_ENVELOPE_V1.md` is consumed through an exact-version fixture at both boundaries. The contract is agreed; adapter conformance and real-device readiness are not proven. W3/W4 follow usable observation and labeling data. New worker placement, pickup, execution start, source review, merge and production proof remain separate events. This receipt creates no Executive Job/Attempt or worker assignment.

W0 source operation: `jimu-w0-offline-replay-20260906-sol-001`.
